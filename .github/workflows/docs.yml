name: Publish documentation

on:
  push:
    branches: [main]
    paths:
      - 'website/docs/**'
      - 'conformance/upstream-results/**'
      - 'conformance/upstream/catalog.json'
      - 'conformance/upstream/sources.json'
      - 'conformance/data/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: documentation-${{ github.ref }}
  cancel-in-progress: false

jobs:
  publish:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: websites
      url: https://docs.orchiddb.com/
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install mdBook
        run: |
          bash website/docs/install-mdbook.sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - name: Authenticate with personal AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.ORCHIDDB_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.ORCHIDDB_AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      - name: Build and publish documentation and committed results
        run: bash website/docs/deploy.sh
      - name: Record published documentation
        run: |
          echo 'Published https://docs.orchiddb.com/' >> "$GITHUB_STEP_SUMMARY"
