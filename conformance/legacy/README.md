# Archived bespoke probes

These historical authored probes and results are retained for provenance and
supplemental regression investigation. They are **not** the current conformance
comparison, and their counts do not appear in the upstream report.

Use `conformance/run.py` and `conformance/upstream/` for the primary comparison.
