# Mock client downloads

These archives are download placeholders for the installation UI, not working
libraries. Each contains only a README and a machine-readable `placeholder.json`.
The release workflow regenerates these same assets for GitHub releases. Replace
these placeholders with tested SDK packages when those libraries are released.
